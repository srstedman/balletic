{
  "version": "0.0.1",
  "scripts": {
    "build": "rm -rf dist; tsc --build tsconfig.json",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^26.4.1",
    "jest": "^30.5.1",
    "ts-jest": "^29.4.12",
    "typescript": "^6.0.2"
  }
}